# svc.yaml
apiVersion: v1
kind: Service
metadata:
  namespace: gin-mall
  name: gin-mall-svc
spec:
  selector:
    app: gin-mall
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP  # 或 NodePort / LoadBalancer（根据需求）
